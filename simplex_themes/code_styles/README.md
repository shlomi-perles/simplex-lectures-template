# Custom code styles

Save Theme Studio code-style exports here.

Files should be Python modules that define one or more Pygments `Style`
subclasses. Re-run `simplex theme-studio` after adding a file to see it in the
code-style list.
